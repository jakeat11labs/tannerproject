node_modules/
.env
voices.config.json
.cache/
out/
# Exception: allow training clips for GitHub Pages
!projects/interview-audio/out/unique-impact-clip.mp3
!projects/interview-audio/out/ai-agents-daily-use-clip.mp3
!projects/interview-audio/out/why-elevenlabs-deep-dive-clip.mp3
*.log
.DS_Store

# Claude Code project state — ignore everything except the committed skills
.claude/*
!.claude/skills/
!.claude/skills/**

# Raw session source material is private by default.
# Force-add only after the user confirms the contents are safe to publish.
sessions/*/raw-transcript/
sessions/*/claude-code-transcript/
sessions/*/source/
sessions/*/_source/
sessions/*/private/
